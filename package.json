{
  "name": "devdojo-components-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build system for DevDojo Components' JavaScript-backed components (Monaco, Tiptap).",
  "scripts": {
    "build": "NODE_ENV=production node esbuild.config.js",
    "dev": "node esbuild.config.js --watch"
  },
  "dependencies": {
    "@tiptap/core": "^2.0.0",
    "@tiptap/extension-link": "^2.0.0",
    "@tiptap/extension-underline": "^2.27.2",
    "@tiptap/pm": "^2.0.0",
    "@tiptap/starter-kit": "^2.0.0",
    "monaco-editor": "^0.55.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
