/vendor
/node_modules
composer.lock
package-lock.json
.phpunit.result.cache
.phpunit.cache

# Compiled editor bundles (monaco/tiptap) are generated by `npm run build`.
# They are intentionally committed so `vendor:publish --tag=devdojo-assets`
# works out of the box after `composer require`. Delete this comment block and
# uncomment the next line if you prefer to build them in CI instead.
# /public/devdojo
