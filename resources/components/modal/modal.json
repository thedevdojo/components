{
    "name": "modal",
    "label": "Modal",
    "description": "An accessible, teleported dialog with focus trapping, header/content/footer slots, and event-driven open/close.",
    "category": "Overlays",
    "dependencies": ["button"],
    "props": [
        { "name": "open", "type": "boolean", "default": false, "description": "Whether the modal is open on load." },
        { "name": "closeButton", "type": "boolean", "default": true, "description": "Show the top-right close button." },
        { "name": "zIndex", "type": "text", "default": "50", "description": "The stacking z-index for the overlay." }
    ],
    "slots": [
        { "name": "trigger", "description": "Custom element that opens the modal." },
        { "name": "header", "description": "Modal heading.", "default": "Modal Title" },
        { "name": "content", "description": "Main modal body.", "default": "The main body content for your modal will go here." },
        { "name": "footer", "description": "Action row, usually buttons." }
    ],
    "studio": {
        "container": "<div class=\"flex min-h-96 w-full items-center justify-center\">{{component}}</div>",
        "defaults": { "open": true },
        "height": "28rem"
    }
}
